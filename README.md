outgoing:
	РезультатЗапроса = Запрос.Выполнить();
	
	ТаблицаРезультата = запрос.Выполнить().Выгрузить();
	Хранилище = новый ХранилищеЗначения(ТаблицаРезультата);
	
	Возврат Хранилище;

upgoing:
&НаСервере
Функция  ПолучитьНаСервере()

	Подключение = ПодключениеКWS();	
	Хранилище = Подключение.Бригады();
	Таблица = Хранилище.Получить();
	Возврат Таблица;  
	
КонецФункции // ПолучитьНаСервере()  

&НаСервере
Функция ПодключениеКWS()

	АдресWS = "http://192.168.1.47/tradeTest/ws/ОбменWS.1cws?wsdl";
	ИмяСервиса = "ОбменWS";                                                
	URIПространстваИменСервиса = "1.1.1.1";
	
	ЗащищенноеСоединение = новый ЗащищенноеСоединениеOpenSSL;
	
	WSОпределение = новый WSОпределения(АдресWS,,,,,ЗащищенноеСоединение);
	WSПрокси = новый WSПрокси(WSОпределение,URIПространстваИменСервиса,ИмяСервиса,ИмяСервиса+"Soap");
	
	Возврат WSПрокси;	
	

КонецФункции // подключениеКWS()
